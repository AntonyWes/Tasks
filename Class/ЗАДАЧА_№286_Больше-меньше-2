#include <iostream>
#include <string>

using namespace std;

int main()
{
	string a;
	string b;
	int aAmass;
	int bAmass;
	cin >> a;
	cin >> b;
	int ASize = a.size();
	int BSize = b.size();
	int MinSize;
	int DotIndexA = string::npos;
	int DotIndexB = string::npos;
	if (a[0] == '-' && b[0] != '-')
	{
		cout << "<";
		return 0;
	}
	else if (a[0] != '-' && b[0] == '-')
	{
		cout << ">";
		return 0;
	}
	else if (a[0] != '-' && b[0] != '-')
	{
		DotIndexA = a.find('.');
		DotIndexB = b.find('.');
		if (DotIndexA == string::npos) { DotIndexA = ASize; }
		if (DotIndexB == string::npos) { DotIndexB = BSize; }
		if (DotIndexA > DotIndexB) { cout << ">"; return 0; }
		else if (DotIndexA < DotIndexB) { cout << "<"; return 0; }
		else {
			a.erase(DotIndexA, 1);
			b.erase(DotIndexB, 1);
			if (ASize > BSize) { MinSize = BSize; }
			else { MinSize = ASize; }
			for (int i = 0; i < MinSize; i++)
			{
				if (a[i] > b[i]) { cout << ">"; return 0; }
				else if (a[i] < b[i]) { cout << "<"; return 0; }
			}
			if (ASize > BSize) { cout << ">"; return 0; }
			else if (ASize < BSize) { cout << "<"; return 0; }
		}
		cout << "=";
		return 0;
	}
}
/*
2.39 3.61	<
123 12.3	>
12345678 12345678.0	=
-1.0 1.0 <
*/
